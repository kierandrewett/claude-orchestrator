services:
    claude:
        build:
            context: .
            dockerfile: Dockerfile
        ports:
            - "${PORT:-8080}:8080"
        volumes:
            - claude_data:/app/data
            - ./config:/app/config:ro
        restart: unless-stopped

volumes:
    claude_data:
