[package]
name = "backend-traits"
version = "0.1.0"
edition = "2021"

[dependencies]
claude-events = { path = "../events" }
tokio         = { version = "1", features = ["full"] }
anyhow        = "1"
async-trait   = "0.1"
